// Definición de la clase principal del programa
public class App {
    public static void main(String[] args) {
        Vista vista = new Vista();
        vista.mostrarMenu();  // Llamamos al menú de la vista para que se inicie el programa
    }
}
